services:
  - type: web
    name: board-vision-backend
    env: docker
    region: oregon
    plan: free
    rootDir: board-vision-app
    dockerfilePath: Dockerfile
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: board-vision-db
          property: connectionString
      - key: GROQ_API_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: MAIL_USERNAME
        sync: false
      - key: MAIL_PASSWORD
        sync: false
      - key: HUGGINGFACE_TOKEN
        sync: false

databases:
  - name: board-vision-db
    databaseName: clens_chess_db
    user: boardvision
    plan: free
    region: oregon
